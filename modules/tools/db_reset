#! /bin/bash  

dir=$( cd "$( dirname "$0" )" && cd ../../ && pwd )
date=`date +%y%m%d-%H%M`

if [[ "$1" == git ]]; then 
    cd $dir && git reset --hard && cd -
fi

rm -rf $dir/dbf/*.db
rm -rf $dir/tmp
mkdir $dir/tmp

rm -rf $dir/dbf/nettemp.db
sqlite3 -cmd ".timeout 2000" $dir/dbf/nettemp.db < $dir/modules/tools/nettemp.schema
chmod 775 $dir/dbf/nettemp.db
chown root.www-data $dir/dbf/nettemp.db













