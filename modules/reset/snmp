#! /bin/bash  

dir=$(cd "$( dirname "$0" )" && cat ../../conf.php |grep global | awk -F"'" '{ print $2 }')
date=`date +%y%m%d-%H%M`

name="snmp"

rm -rf $dir/dbf/$name.db

sqlite3 $dir/dbf/$name.db  "CREATE TABLE snmp (
  id INTEGER PRIMARY KEY,
  name UNIQUE,
  community TEXT,
  host TEXT,
  oid TEXT,
  divider TEXT
  );"

chmod 775 $dir/dbf/snmp.db
chgrp www-data $dir/dbf/$name.db